import Random;

func main() -> int
    int guess := scan();
    int secret_number := Random::integer(0, 100);
    while guess != secret_number do
        if guess < secret_number then
            print(1);
        else
            print(-1);
        end
        guess := scan();
    end
    print(0);
end